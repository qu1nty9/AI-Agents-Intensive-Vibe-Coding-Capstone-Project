# Sample Reports

Generated sample reports are committed here when they are useful for the Kaggle writeup or video.

Runtime-generated reports should go under `reports/generated/` or `runs/`, both of which are ignored by git.

Generate the current sample report:

```bash
make sample-report
```

Generate the benchmark summary:

```bash
make benchmark-report
```
