# Clean Reproducible Baseline

This is the happy-path benchmark case.

Expected behavior:

- the script runs;
- it reports `accuracy = 0.9`;
- the claim expects `accuracy = 0.9`;
- the final verdict should be `reproduced`.

