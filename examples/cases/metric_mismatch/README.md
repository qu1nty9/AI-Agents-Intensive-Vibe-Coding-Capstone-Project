# Metric Mismatch

This case has an overstated claim.

Expected behavior:

- the script runs;
- it reports `accuracy = 0.75`;
- the claim expects `accuracy = 0.95` with `0.01` tolerance;
- the final verdict should be `not_reproduced`.

